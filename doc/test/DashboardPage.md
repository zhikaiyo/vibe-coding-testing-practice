---
description: DashboardPage 測試案例
---

> 狀態：初始為 [ ]、完成為 [x]
> 注意：狀態只能在測試通過後由流程更新。
> 測試類型：前端元素、function 邏輯、Mock API、驗證權限...

---

## [x] 【前端元素】渲染載入中狀態
**範例輸入**：元件剛掛載，等待 `productApi.getProducts()` 回應期間  
**期待輸出**：畫面顯示「載入商品中...」字樣與載入動畫（`loading-spinner`）。

---

## [x] 【前端元素】渲染管理員資訊與專屬連結
**範例輸入**：`user` 的 `role` 為 `admin`，`username` 為 `AdminUser`  
**期待輸出**：顯示「Welcome, AdminUser 👋」與「管理員」標籤，且上方導覽列顯示「🛠️ 管理後台」的連結。

---

## [x] 【前端元素】渲染一般用戶資訊
**範例輸入**：`user` 的 `role` 為 `user`  
**期待輸出**：顯示「一般用戶」標籤，且上方導覽列**不**顯示「🛠️ 管理後台」的連結。

---

## [x] 【Mock API】成功獲取並顯示商品列表
**範例輸入**：`productApi.getProducts()` 成功回傳包含多筆商品的陣列資料  
**期待輸出**：結束載入中狀態，畫面正確渲染出商品卡片，包含商品名稱、描述與價格。

---

## [x] 【Mock API】獲取商品失敗顯示錯誤訊息
**範例輸入**：`productApi.getProducts()` 回傳 500 錯誤與錯誤訊息（例如「伺服器異常」）  
**期待輸出**：結束載入中狀態，畫面顯示錯誤區塊與對應的錯誤訊息。

---

## [x] 【Mock API】獲取商品時遇到 401 錯誤處理
**範例輸入**：`productApi.getProducts()` 回傳 401 狀態碼  
**期待輸出**：不顯示錯誤訊息（由攔截器統一處理），維持無錯誤狀態。

---

## [x] 【function 邏輯】執行登出操作
**範例輸入**：點擊「登出」按鈕  
**期待輸出**：成功呼叫 `logout` 函數，且自動導向至 `/login` 頁面。
