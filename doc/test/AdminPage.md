---
description: AdminPage 測試案例
---

> 狀態：初始為 [ ]、完成為 [x]
> 注意：狀態只能在測試通過後由流程更新。
> 測試類型：前端元素、function 邏輯、Mock API、驗證權限...

---

## [x] 【前端元素】渲染管理後台頁面
**範例輸入**：無  
**期待輸出**：畫面成功渲染，包含「🛠️ 管理後台」標題、返回連結，以及「管理員專屬頁面」資訊區塊。

---

## [x] 【前端元素】顯示管理員標籤
**範例輸入**：`user` 的 `role` 為 `admin`  
**期待輸出**：頂部顯示 `role-badge`，且文字內容為「管理員」。

---

## [x] 【前端元素】顯示一般用戶標籤
**範例輸入**：`user` 的 `role` 為 `user` 或其他非 admin 角色  
**期待輸出**：頂部顯示 `role-badge`，且文字內容為「一般用戶」。

---

## [x] 【function 邏輯】點擊返回連結
**範例輸入**：點擊「← 返回」  
**期待輸出**：觸發路由導向至 `/dashboard` 頁面。

---

## [ ] 【Mock API】執行登出操作
**範例輸入**：點擊「登出」按鈕  
**期待輸出**：成功呼叫 `logout` 函數，且自動導向至 `/login` 頁面（並清除 state）。
